# Domain name — Caddy auto-provisions HTTPS certificates for this domain.
# Use "localhost" for local testing (self-signed cert).
SITE_ADDRESS=food.example.com

# Path to Google service account credentials JSON on the host.
GOOGLE_APPLICATION_CREDENTIALS_FILE=./credentials.json
